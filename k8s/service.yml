apiVersion: v1
kind: Service
metadata:
  name: todo-app
spec:
  selector:
    app: todo-app
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
  clusterIP: 10.0.171.239
  type: LoadBalancer
